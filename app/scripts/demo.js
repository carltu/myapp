// i should get a confilict here
var test = 'this is a new merging test for file';
var test3 = 'test2';

var vivian2 = 'this is a new merging test for an old file';
// this is a new change
var kevin = 'change from kevin to carl';
var newVar = 'robbie';
if (1 ==1) {
    var newVar2 = 'carl';
    var newVar3 = 'carl 2';
}

var newVar4 = 'robbie 2';
var kevin2 = 'kevin2';
$timeout(function() {
    // iOS permission issue
    if (message === 'has no access to assets') {
      $cordovaDialogs.alert('Cancelled taking a picture', null, 'Camera');
    }
    else {
      $cordovaDialogs.alert(message, null, 'Camera');
    }
  }, 0);
